version: 0.2
install:
    runtime-versions:
      nodejs: 16
      commands:
      - npm install
      
phases:
  pre_build:
    commands:
      - npm install --global yarn

  build:
    commands:
      - yarn install 
      - yarn lint
      - yarn run web

on_success:
  commands:
    - echo "Web build run successfully!"

on_failure:
  commands:
    - echo "Web build failed."

cache:
  paths:
    - node_modules/
